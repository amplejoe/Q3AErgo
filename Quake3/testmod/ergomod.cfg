seta g_gametype 0

// bots will use grapple hook (if enabled)
bot_grapple 1		

// enable weapon autoswitch (mod enables smart switch)
seta cg_autoswitch 1

// map loop (currently only 1)
set v1 "vstr map; set nextmap vstr v1"
vstr v1

// **************** old stuff ********************

//seta sv_maxclients 2 // limits max clients to 2
// set fraglimit 5  // set in testmod
// seta rconpassword "pass" // needed to use rcon command

// bot_minplayers 2 // this is used to ensure a minimum numbers of players are playing on a server bots are added/removed to get the specified number of players in the game (sv_maxclients not necessarily needed). Is SET in testmod.bat

// bot_grapple 1		// bots will use grapple hook  
// bot_usehook 1	// same?

// g_spSkill "2" 	// holds your current skill level for single player 1 = I can win 2 = bring it on 3 = hurt me plenty 4 = hardcore and 5 = nightmare (bots will be added like this) set in testmod.bat

//set sv_maxclients 2
//bot_minplayers 5
// set v2 "devmap q3dm2; set nextmap vstr v1"